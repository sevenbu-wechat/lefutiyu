<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="SIGN_UP_USER">
    <insert id="insert" parameterType="SignUpUserDTO">
        <selectKey resultType="java.lang.Integer" order="AFTER" keyProperty="id">
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert into SIGN_UP_USER(user_name, train_base_schedule_id, age, height, mobile, channel, remark)
        values(
        #{userName},
        #{trainBaseScheduleId},
        #{age},
        #{height},
        #{mobile},
        #{channel},
        #{remark}
        );

    </insert>
</mapper>
